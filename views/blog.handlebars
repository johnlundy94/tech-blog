<div class="text-center">
  <h2>{{name}}</h2>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  <p>{{description}}</p>

  <p>
    <button class="btn btn-primary">Post to the blog!</button>
  </p>
</div>